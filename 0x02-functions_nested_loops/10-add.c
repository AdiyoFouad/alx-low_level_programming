#include "main.h"

/**
 * add - function that adds two integers and returns the result
 * @n: first argument
 * @m: second argument
 * Return: the result of n plus m
 */

int add(int n, int m)
{
	return (n + m);
}
